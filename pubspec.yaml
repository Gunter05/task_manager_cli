name: task_manager_cli
description: Application CLI de gestion de tâches en Dart pur avec stockage JSON local.
version: 1.0.0
sdk: '^3.0.0'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # Pas de dépendances externes nécessaires

dev_dependencies:
  lints: ^3.0.0